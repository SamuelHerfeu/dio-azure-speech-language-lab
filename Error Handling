try {
    DocumentSentiment analysis = SentimentAnalyzer.analyzeSentiment(text);
    // Process results
} catch (HttpResponseException e) {
    System.err.println("API Error: " + e.getMessage());
} catch (Exception e) {
    System.err.println("General error: " + e.getMessage());
}
